<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>TODO App</title>
</head>
<body>

    <div class="container">
        <h1>Todo App</h1>
        <hr>
        <form action="/" method="POST">
            <label for="todoDescription">DESCRIPTION</label>
            <input type="text" name="description" id="todoDescription" placeholder="What do you need to do?">
            <hr>
            <div class="row">
                <div class="col-6">
                    <label for="todoCategory">CATEGORY</label>
                    <select name="category" id="todoCategory">
                        <option value="" disabled selected>Choose a Category</option>
                        <option value="personal">Personal</option>
                        <option value="work">Work</option>
                        <option value="school">School</option>
                        <option value="cleaning">Cleaning</option>
                        <option value="others">Others</option>
                    </select>
                </div>
                <div class="col-6">
                    <label for="todoDate">DUE DATE</label>
                    <input type="date" id="todoDate" name="date">
                </div>
            </div>
            <hr>
            <div class="buttons">
                <button type="submit" id="add-task">Add Task</button>
            </div>
        </form>
        <form action="/delete" method="POST">
            <button type="submit" id="delete-task">Delete Selected</button>
            <ul id="todolist">
                <% for(let i = 0; i < todoList.length; i++) { %>
                    <li>
                        <input type="checkbox" name="tasksToDelete" value="<%= i %>">
                        <%= todoList[i].description %>
                        <%= todoList[i].date %>
                        <%= todoList[i].category %>
                    </li>
                <% } %>
            </ul>
            
        </form>
    </div>
</body>
</html>
